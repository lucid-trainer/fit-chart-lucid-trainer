{
  "name": "fit-chart-batch",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-charts": "node build-stages && node build-dream && node build-accel-pos && node build-accel-act && node build-gyro-act && node build-hr && node build-hr-var && node build-hr-var-trend"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "math.js": "^1.1.46",
    "mathjs": "^11.11.2"
  }
}
